# ============================================================
# BotKeuangan — Environment Variables
# Salin file ini menjadi .env dan isi sesuai konfigurasi kamu
# ============================================================

# ── PostgreSQL ────────────────────────────────────────
POSTGRES_USER=finance_user
POSTGRES_PASSWORD=ganti_password_ini_yang_kuat
POSTGRES_DB=finance_db

# ── Google Gemini API ─────────────────────────────────
# Dapatkan di https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ── WhatsApp ──────────────────────────────────────────
# Nomor WA pemilik (format: 628xxxxxxxxxx, tanpa + atau -)
OWNER_WA_NUMBER=628123456789

# ── Dashboard ─────────────────────────────────────────
# URL backend yang bisa diakses browser (bukan internal Docker)
# Jika pakai reverse proxy, ganti ke https://api.domain-kamu.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:3002

# Port dashboard yang di-expose ke host
DASHBOARD_PORT=3000

# Port backend API yang di-expose ke host
BACKEND_PORT=3002
